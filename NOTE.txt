1 présenter la classe Contact service
    On a un repository de contact avec une fonction qui permet de récupérer à partir de son ID un contact. ce repository
    est écrit en Java et on sait que lorsque l'ID n'existe pas alors la fonction renvoie une référence NULL

     -- Commenter le code :
        pattern if null qui permet d'éviter les nullepointers exception
        problème : la manipulation de référence null dans le code ce n'est pas très propre
                   et cela introduit de la complexité avec les structures if

2 Pour améliorer la situation, on va introduire le type Option (encaspulation du retour de l'appel de la fonction
    du repository dans une Option
    Donc ce n'est plus un contact que notre service renvoie, mais une Option de Contact. 